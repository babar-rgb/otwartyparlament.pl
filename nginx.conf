server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    
    # Optional: Proxy /api to backend if needed, but we use direct PostgREST usually.
}
